id = scene.CreateEntity()
scene.SetComponent(id, "position")
scene.SetComponent(id, "mesh", 1, 1)
scene.SetComponent(id, "velocity")					
scene.SetComponent(id, "playerComponent", 0, 0, 255) -- Colour of player ball